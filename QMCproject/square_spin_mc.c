#include <stdio.h>

/* This function initializez a square latice of spins with random initial conditions */
int * init_config(length,width)
{
    // define array to hold first values
    int init_arr[length][width];
    
    // loop over all indicies and place a 1 or 0 for spin up and down respectively
    for (int i = 0; i < length; i += 1)
    {
        for (int j = 0; j < width; j+=1)
        {
            // Now we are at a particular point so asign it a random configuratoin

        }
    }

}

int main(void)
{

}
